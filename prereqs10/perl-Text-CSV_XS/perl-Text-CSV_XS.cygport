CPAN_AUTHOR=hmbrand
inherit perl

NAME="perl-Text-CSV_XS"
VERSION=1.29
RELEASE=2
CATEGORY="Perl"
SUMMARY="Perl comma-separated values manipulation XS backend"
DESCRIPTION="Text::CSV_XS provides facilities for the composition and
decomposition of comma-separated values.  An instance of the Text::CSV_XS
class can combine fields into a CSV string and parse a CSV string into fields."
SRC_URI="${SRC_URI%.tar.gz}.tgz"
